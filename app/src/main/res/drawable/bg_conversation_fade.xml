<?xml version="1.0" encoding="utf-8"?>
<!-- ~5dp fade strip at the very top of the conversation area:
     transitions from transparent (camera fully visible) into the solid
     themed conversation box beneath it. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:startColor="#00000000"
        android:endColor="@color/theme_conversation_panel"
        android:angle="270" />
</shape>
