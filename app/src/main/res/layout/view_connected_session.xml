<?xml version="1.0" encoding="utf-8"?>
<!-- Full-screen blue Connected Session page shown after a successful Dual Device
     connection. Shows the real session code and an action button (Host: End
     Session / Joiner: Disconnect), plus a circular X exit. The X is positioned
     safely above the nav bar via the runtime nav-bar insets. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/akai_blue"
    android:clickable="true"
    android:focusable="true">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <TextView
            android:id="@+id/tvConnectedTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/poppins_regular"
            android:text="@string/dual_connected_title"
            android:textColor="@color/white"
            android:textSize="26sp" />

        <TextView
            android:id="@+id/tvConnectedCode"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="28dp"
            android:fontFamily="@font/poppins_bold"
            android:letterSpacing="0.05"
            android:textColor="@color/white"
            android:textSize="50sp" />

        <TextView
            android:id="@+id/btnConnectedAction"
            android:layout_width="wrap_content"
            android:layout_height="56dp"
            android:layout_marginTop="52dp"
            android:minWidth="180dp"
            android:gravity="center"
            android:fontFamily="@font/poppins_bold"
            android:textColor="@color/akai_blue"
            android:textSize="18sp"
            android:background="@drawable/bg_btn_white_3d"
            android:foreground="?attr/selectableItemBackground"
            android:clickable="true"
            android:focusable="true" />

    </LinearLayout>

    <!-- Circular white 3D X button, raised above the navigation / gesture bar. -->
    <ImageButton
        android:id="@+id/btnConnectedExit"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="40dp"
        android:background="@drawable/bg_circle_white_3d"
        android:contentDescription="@string/dual_connected_exit"
        android:scaleType="centerInside"
        android:paddingStart="16dp"
        android:paddingTop="13dp"
        android:paddingEnd="16dp"
        android:paddingBottom="19dp"
        android:src="@drawable/ic_dual_device_exit"
        android:clickable="true"
        android:focusable="true" />

</FrameLayout>
